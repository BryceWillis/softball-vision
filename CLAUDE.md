# Softball Vision

The working agreements for this repository live in `AGENTS.md`, so that every agent — Claude, Codex, or otherwise — reads the same file and there is only one to keep current.

@AGENTS.md

**In short, before you do anything else:**

- The design and roadmap are in `../obsidian-projects/softball-vision-docs/`; the shared process is in `../obsidian-projects/_process/`. **Read only those two** — the vault holds other, unrelated projects.
- **Never commit your own work.** The reviewer commits, and only on approval.
- **Never commit a real player name.** Sanitized placeholders only, everywhere.
- Check `CHANGE-REQUESTS.md` in the notes folder for the change currently in flight.
